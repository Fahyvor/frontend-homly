// import { SetSearchResultsAction, SearchActionTypes, SearchState } from '../types/searchTypes';
// import { Action } from 'redux';

// export const setSearchResults = (results: any[]): Action<string> => ({
//   type: 'SET_SEARCH_RESULTS',
//   payload: results,
// });


// const initialState: SearchState = {
//   searchResults: [],
// };

// const searchReducer = (state = initialState, action: SearchActionTypes): SearchState => {
//   switch (action.type) {
//     case 'SET_SEARCH_RESULTS':
//       return {
//         ...state,
//         searchResults: action.payload,
//       };
//     default:
//       return state;
//   }
// };

// export default searchReducer;
